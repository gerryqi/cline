// Intentionally thin in the first iteration.
// Desktop integration can adopt this surface after command transport is consolidated.
export interface DesktopAdapterOptions {
	workspaceRoot: string;
}
