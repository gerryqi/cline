# @cline/agents Architecture

Architecture docs are centralized:

- [`packages/ARCHITECTURE.md`](../ARCHITECTURE.md)

This package participates as the stateless agent runtime layer (tool loop, hooks/extensions, teams). See centralized architecture docs for boundaries and flows.
