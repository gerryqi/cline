{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run Cline RPC Chat Extension",
			"type": "extensionHost",
			"request": "launch",
			"args": ["--extensionDevelopmentPath=${workspaceFolder}"],
			"preLaunchTask": "build-extension"
		}
	]
}
